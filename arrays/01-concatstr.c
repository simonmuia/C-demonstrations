#include <stdio.h>

/**
 * Program to concat series of inputs on string
 * main - entry point
 * Return: 0 always
*/

int main ()
{
    char name[5] = {'S', 'i', 'm', 'o', 'n'};
    // print the string
    printf("String combined : %s\n", name);
    return 0;
}